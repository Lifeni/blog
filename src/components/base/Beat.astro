<div data-beat aria-hidden="true">
  <span></span>
  <span></span>
  <span></span>
</div>

<style lang="less">
  div {
    padding: 1.5rem 0 0.5rem;
    display: flex;
    align-items: flex-end;

    span {
      width: 0.25rem;
      height: 0.75rem;
      margin: 0 0.125rem;
      border-radius: 1px;
      background-color: var(--cover-color);
      transform-origin: bottom;

      &:nth-child(1) {
        animation: loading 0.75s infinite 0.25s;
      }

      &:nth-child(2) {
        animation: loading 0.75s infinite 0.75s;
      }

      &:nth-child(3) {
        animation: loading 0.75s infinite 0.5s;
      }
    }

    &[data-pause] {
      span {
        animation-play-state: paused;
      }
    }

    @keyframes loading {
      0% {
        transform: scaleY(1);
      }
      50% {
        transform: scaleY(2);
      }
      100% {
        transform: scaleY(1);
      }
    }
  }
</style>
