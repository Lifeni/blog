<div class:list={['group', 'pointer-events-auto']}>
  <slot name="@" />
  <div
    class:list={[
      'invisible group-focus-within:visible',
      'fixed top-0 left-0 z-100',
      'w-screen h-screen',
      'opacity-0 group-focus-within:opacity-100 transition-all',
      'bg-[rgba(0,0,0,0.5)]',
    ]}
  >
  </div>
  <div
    class:list={[
      'invisible group-focus-within:visible',
      'fixed top-0 left-0 z-150',
      'w-screen h-screen',
      'overflow-hidden',
    ]}
  >
    <div
      tabindex="-1"
      class:list={[
        'absolute left-[50%] top-[50%] z-150',
        'w-[calc(100dvw-(--spacing(8)))] max-w-120 h-fit max-h-[calc(100dvh-(--spacing(44)))]',
        'flex flex-col gap-4',
        'bg-white dark:bg-neutral-850',
        'rounded-2xl',
        'shadow-none group-focus-within:shadow-2xl',
        '-translate-x-[50%] -translate-y-[50%]',
        'opacity-0 group-focus-within:opacity-100',
        'transition-all duration-300 ease-in-out',
        'outline-none',
        'overflow-y-auto',
      ]}
    >
      <slot name="<#>" />
    </div>
  </div>
</div>
