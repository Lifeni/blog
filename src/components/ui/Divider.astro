---
export interface Props {
  color?: 0 | 1
  direction?: '|' | '-'
}

const { color = 0, direction = '-' } = Astro.props
---

<hr
  class:list={[
    direction === '-'
      ? 'w-full border-t'
      : 'h-full min-h-[1rem] inline-flex border-t-none border-l',
    color === 1
      ? 'border-light-100 dark:border-dark-700'
      : 'border-light-100 dark:border-dark-800',
  ]}
/>
