---
const props = Astro.props
---

<div class:list={['wrapper', 'flex', 'animate-fade-in']}>
  <img
    src={props.src}
    alt={props.alt}
    title={props.alt}
    loading="lazy"
    class:list={['medium-zoom', 'w-full h-auto', 'rounded-md']}
  />
</div>
