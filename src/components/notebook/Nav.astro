---
import Button from '@components/Button.astro'

const navs = [
  { name: '文章', icon: 'i-ic-round-menu-book' },
  { name: '专题', icon: 'i-ic-round-question-answer' },
  { name: '存档', icon: 'i-ic-round-inbox' },
]
---

<nav
  pos="fixed right-0 bottom-0 lg:left-0 lg:right-unset lg:bottom-unset lg:top-1/2"
  transform="none lg:~ lg:translate-y--1/2"
>
  <ul p="6" flex="~ col gap-3 md:gap-4 items-end lg:items-start">
    {
      navs.map(({ name, icon }) => (
        <li>
          <Button {name} {icon} href={`/#${name}`} />
        </li>
      ))
    }
  </ul>
</nav>
